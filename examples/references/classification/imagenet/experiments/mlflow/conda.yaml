name: imagenet_env
channels:
  - pytorch
dependencies:
  - python=3.7
  - numpy
  - cudatoolkit
  - pytorch
  - torchvision
  - pip
  - pip:
      - mlflow
      - albumentations
      - tqdm
      - tensorboardX
      - py_config_runner
      - pynvml
      - pytorch-ignite
      - git+https://github.com/vfdev-5/ImageDatasetViz.git
